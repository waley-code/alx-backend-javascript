import handleResponseFromAPI from "../2-then"

describe('handleResponseFromAPI', () => {
  test('should resolve promise and return response object', async () => {
    expect(true).toEqual(true);
  });
});
